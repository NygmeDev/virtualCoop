def localProperties = new Properties()
def localPropertiesFile = rootProject.file('local.properties')
if (localPropertiesFile.exists()) {
    localPropertiesFile.withReader('UTF-8') { reader ->
        localProperties.load(reader)
    }
}

def flutterRoot = localProperties.getProperty('flutter.sdk')
if (flutterRoot == null) {
    throw new GradleException("Flutter SDK not found. Define location with flutter.sdk in the local.properties file.")
}

def flutterVersionCode = localProperties.getProperty('flutter.versionCode')
if (flutterVersionCode == null) {
    flutterVersionCode = '1'
}

def flutterVersionName = localProperties.getProperty('flutter.versionName')
if (flutterVersionName == null) {
    flutterVersionName = '1.0'
}

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

android {
    compileSdkVersion 30

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    lintOptions {
        disable 'InvalidPackage'
        checkReleaseBuilds false
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId "com.virtual_coop"
        minSdkVersion 16
        targetSdkVersion 30
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    signingConfigs {
        solAndesRelease {
            keyAlias 'keySolAndes'
            keyPassword 'solAndes**'
            storeFile file('D:/RecursosVirtualCoop/keys/solAndes.jks')
            storePassword 'solAndes**'
        }
        lasNavesRelease {
            keyAlias 'keyLasNaves'
            keyPassword 'lasNaves'
            storeFile file('D:/RecursosVirtualCoop/keys/lasNaves.jks')
            storePassword 'lasNaves'
        }
        ciudadZamoraRelease {
            keyAlias 'keyCiudadZamora'
            keyPassword 'ciudadZamora'
            storeFile file('D:/RecursosVirtualCoop/keys/ciudadZamora.jks')
            storePassword 'ciudadZamora'
        }
        chunchiRelease {
            keyAlias 'keyChunchi'
            keyPassword 'chunchi**'
            storeFile file('D:/RecursosVirtualCoop/keys/chunchi.jks')
            storePassword 'chunchi**'
        }
        cCStoDomingoRelease {
            keyAlias 'keyCCStoDomingo'
            keyPassword 'cCStoDomingo**'
            storeFile file('D:/RecursosVirtualCoop/keys/cCStoDomingo.jks')
            storePassword 'cCStoDomingo**'
        }
        devRelease {
            keyAlias 'keyCCStoDomingo'
            keyPassword 'cCStoDomingo**'
            storeFile file('D:/RecursosVirtualCoop/keys/cCStoDomingo.jks')
            storePassword 'cCStoDomingo**'
        }
    }

    flavorDimensions "dimensions"
    productFlavors {
        def solAndesSigningVariable = signingConfigs.solAndesRelease
        def lasNavesSigningVariable = signingConfigs.lasNavesRelease
        def ciudadZamoraSigningVariable = signingConfigs.ciudadZamoraRelease
        def chunchiSigningVariable = signingConfigs.chunchiRelease
        def cCStoDomingoSigningVariable = signingConfigs.cCStoDomingoRelease
        def devSigningVaribale = signingConfigs.devRelease

        solAndes {
            dimension "dimensions"
            applicationIdSuffix ".solAndes"
            versionCode 1
            versionName "1.0.0"
            signingConfig solAndesSigningVariable
        }
        lasNaves {
            dimension "dimensions"
            applicationIdSuffix ".lasNavesNew"
            versionCode 3
            versionName "1.0.2"
            signingConfig lasNavesSigningVariable
        }
        ciudadZamora {
            dimension "dimensions"
            applicationIdSuffix ".ciudadZamora"
            versionCode 3
            versionName "1.0.2"
            signingConfig ciudadZamoraSigningVariable
        }
        chunchi {
            dimension "dimensions"
            applicationIdSuffix ".chunchi"
            versionCode 2
            versionName "1.0.1"
            signingConfig chunchiSigningVariable
        }
        cCStoDomingo {
            dimension "dimensions"
            applicationIdSuffix ".cCStoDomingo"
            versionCode 1
            versionName "1.0.0"
            signingConfig cCStoDomingoSigningVariable
        }
        dev {
            dimension "dimensions"
            applicationIdSuffix ".dev"
            versionCode 1
            versionName "1.0.0"
            signingConfig cCStoDomingoSigningVariable
        }
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig signingConfigs.solAndesRelease
            signingConfig signingConfigs.lasNavesRelease
            signingConfig signingConfigs.ciudadZamoraRelease
            signingConfig signingConfigs.chunchiRelease
            signingConfig signingConfigs.cCStoDomingoRelease
            signingConfig signingConfigs.devRelease

        }
    }
}

flutter {
    source '../..'
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
}
